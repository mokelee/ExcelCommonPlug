
# [0.8.7] - 2026-09-12

- 放大MD行数从 100 -> 200
- 优化md在标题筛选，手动选择多区域的时候处理

# [0.8.6] - 2026-09-12

- 重构「表格转Markdown」取值逻辑：改用 Excel 渲染后的显示文本（Range.Text）逐格取值，日期、时间、百分比、货币、科学计数、分数、前导零、布尔、错误值等均与单元格实际显示一致
- 修复常规格式数字被错误输出为「/通用格式」的问题
- 修复布尔值输出为 True/False（应为 TRUE/FALSE）、错误值可能抛异常的问题
- 新增超链接转换：单元格超链接输出为 [文本](URL) 形式
- 新增合并单元格处理：非左上角单元格填充左上角内容
- 新增列过窄显示为「####」时回退到底层数值的兜底
- 转义美元符号 $ 为 \$，避免被 KaTeX/MathJax 渲染器误当作行内公式
- 筛选或隐藏后转换时，仅输出可见行（跳过筛选隐藏、手动隐藏的行）
- 跳过隐藏列，不输出被隐藏的列
- 支持按住 Ctrl 多选的不连续区域：各区域列位置与列数需完全一致，否则提示错误；一致时按块顺序纵向拼接，整体第一行作为表头

# [0.8.5] - 2026-09-09

- 优化退出时的处理
- 增加清理功能，安装时可以同步清理，保证安装效果
- 优化转markdown处理

# [0.6.5] - 2026-07-29

- 新增「表格转Markdown」功能：将当前工作表内容转换为 Markdown 表格格式并复制到剪贴板
- 自动处理单元格中的管道符和换行符转义
- 使用自定义 md.png 图标

# [0.6.4] - 2026-07-09

- 优化聚光灯在冻结窗格模式下的显示，支持所有可见区域正确高亮
- 优化聚光灯在拆分窗格模式下的显示，仅在选中单元格所在窗格内高亮
- 修复不同显示比例（70%/85%/100%等）下聚光灯位置偏移问题
- 使用 Pane.PointsToScreenPixelsX/Y 精确计算坐标，适配任意 DPI 和缩放
- Excel 失去焦点时自动隐藏聚光灯，恢复焦点时自动显示
- 新增 Logger 日志系统，支持文件日志输出
